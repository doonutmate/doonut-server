spring:
  profiles:
    active: local

# Swagger springdoc-ui Configuration
springdoc:
  swagger-ui:
    path: api
    tags-sorter: alpha
    operations-sorter: alpha
  paths-to-match:
    - /**

server:
  port: 8081
